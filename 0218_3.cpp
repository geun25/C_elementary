#include <stdio.h>
void main() {

	/*
	int num;
	printf("정수입력:");
	scanf("%d", &num);

	if (num % 2) {
		printf("홀수입니다.\n");
	}
	else {
		printf("짝수입니다.\n");
	}
	*/

	/*
	int a, b;
	printf("정수 2개 입력: ");
	scanf("%d%d", &a, &b);

	if (a != b) {
		if (a > b) {
			printf("더 큰 정수는 %d", a);
		}
		else {
			printf("더 큰 정수는 %d", b);
		}
	}
	else {
		printf("두 정수는 같다.\n");
	}
	*/
	
	/*
	double c;
	printf("실수 1개 입력: ");
	scanf("%lf", &c);

	printf("%.2lf의 절대값은 ", c);

	if (c >= 0) {
		printf("%.2lf", c);
	}
	else {
		c *= -1.0; // 0보다 작을경우 -1을 곱해 부호를 바꿈
		printf("%.2lf", c);
	}
	
	*/


	/*
	printf("%.2lf의 절대값은 ", c);
	if (c < 0.0) {
		c *= (-1.0);
	}
	printf("%.2lf입니다.\n", c); // 조건문을 사이에 두어 음수일경우에만 부호 바꿈
	*/
	
}